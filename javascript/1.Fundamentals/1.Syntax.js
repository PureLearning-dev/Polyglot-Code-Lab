// js编译器会自动处理空格
let formatted = true; if(formatted){console.log("The code is easy to read !")}

// 上述代码与一下内容一致
let formatted1 = true;
if(formatted1){
    console.log("The code is easy to read !");
}

// 在日常写代码时都是采用第二种格式，较为规整，便于阅读。

// “语句”用于声明变量或者命令js引擎执行一段任务，使用“;”结尾。
let message = "Welcome to Javascript";
console.log(message);

// “块”中存放一段或多段语句
